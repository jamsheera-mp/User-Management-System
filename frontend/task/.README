



 # authSlice.js
 this file manages authentication state in a Redux store using @reduxjs/toolkit.
Uses createAsyncThunk for async API calls (login, register, logout).

Stores token in localStorage after login/register.

Handles pending, fulfilled, and rejected states properly.

Retrieves user info from localStorage on initialization.

Logs out the user by clearing localStorage.